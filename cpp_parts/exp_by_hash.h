#pragma once

#include "spraygen.h"


// CUR_A1 -> lpStrToHash, CUR_RETD -> result hash
SPRAYABLE_PROC(calchash);


// CUR_A1 -> lpImage, CUR_A2 -> dwHashToFind, CUR_RETD -> export fn ptr
SPRAYABLE_PROC(exp_by_hash);

